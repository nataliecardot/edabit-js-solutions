// Reverse and Capitalize
// https://edabit.com/challenge/nBW6ubsQCurPvhpWc

function reverseCapitalize(str) {
	return str.toUpperCase().split('').reverse().join('').toString()
}

// 1) uppercase
// 2) split into array of single letters (substrings) ('')
// 3) reverse the array (no method to reverse a string)
// 4) join array elems with join() (default separator for join() is a comma - use join('') to join elems w/o separator)
// 5) convert back to string (note by default toString() separates elems w/comma)